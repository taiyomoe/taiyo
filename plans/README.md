# Implementation Plans

Generated by the improve skill against commit `458557fb` on 2026-09-21.

Execute in the order below unless dependencies say otherwise. Each executor:
read the plan fully before starting, honor its STOP conditions, and update your
row when done.

## Execution order & status

| Plan | Title | Priority | Effort | Risk | Depends on | Status |
|------|-------|----------|--------|------|------------|--------|
| 001 | Return resolvable page image URLs from `GET /chapters/:id` | P1 | S | LOW | — | TODO |
| 002 | Persist image file extensions and return cover/banner URLs | P1 | M | MED | 001 | TODO |
| 003 | Stand up the web↔api data layer and prove it on one route | P1 | M | MED | — | TODO |
| 004 | Add the "latest chapters" and personal feed endpoints | P1 | S | LOW | — | TODO |
| 005 | Build the browse → detail → read vertical slice in `apps/web` | P1 | L | MED | 001, 002, 003, 004 | TODO |
| 006 | Remove every comment from the source tree, after harvesting the load-bearing rationale into docs | P2 | L | MED | — | TODO |

Status values: TODO | IN PROGRESS | DONE | BLOCKED (with one-line reason) |
REJECTED (with one-line rationale).

## Dependency notes

- **002 requires 001** because 001 introduces `S3_PUBLIC_URL` and the
  `publicBase` normalization that 002 reuses.
- **003 is independent**, but 001/002 are what make the route it proves
  actually render images.
- **005 requires 001–004** — it is the UI on top of everything they expose, and
  its plan states all four must be DONE first.
- **006 has no code dependency on 001–005, but it must not run concurrently
  with any of them.** It touches ~155 files across every package; a parallel
  branch will conflict on almost anything. Land it either before 001 or after
  005. If it lands *after*, its drift check will flag the files 001–005
  changed — that is expected, and the executor should re-read those files
  against the plan's excerpts rather than stop outright.
- 006 step 7 deletes the `TODO(plan-001)` marker in
  `packages/queue/src/processor.ts` and folds it into plan 001's maintenance
  notes. If 006 runs first, make sure that carry-over actually happened before
  starting 001.

## Scope decisions recorded for plan 006

Both were the owner's calls, made when the plan was written — do not revisit
them without asking:

1. **Harvest, then purge.** Most of this repo's comments record *why*
   (StyleX compiler workarounds, the advisory-lock rationale, the palette
   derivation), not *what*. Steps 1–3 move that knowledge into
   `packages/ui/STYLEX.md`, `docs/engineering-notes.md` and
   `apps/web/AGENTS.md` before any source file is touched.
2. **`packages/db/src/migrations/` and `packages/db/src/seeds/` are exempt.**
   Frozen history; their comments stay exactly as they are.

## Findings considered and rejected

- *Keeping the "why" comments and deleting only the restatements (the literal
  99/1 rule)*: rejected by the owner in favour of a full purge plus the doc
  harvest. Applied literally it would have removed only ~40% of the comments
  here, because this repo's comment population is unusually rationale-heavy.
- *Purging YAML, `.env.example` and `docker-compose.yml` comments*: not code,
  and in those files the `#` lines are the only documentation each key has.
- *Adding a lint rule to enforce comment-freedom*: `oxlint` ships no such rule
  today. The detector script in plan 006 plus the policy added to `README.md`
  are the stand-in.
