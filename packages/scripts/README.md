# `@taiyomoe/scripts`

This package provides a commander-powered CLI tool for managing database operations in the Taiyo project.

## Available Commands

- `migrate` - Run database migrations
- `seed` - Seed the database with initial data

## Usage

```bash
infisical run -- pnpm -F @taiyomoe/scripts start <command>
```
