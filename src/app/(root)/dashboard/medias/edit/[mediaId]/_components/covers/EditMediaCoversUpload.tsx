import type { MediaWithRelations } from "~/lib/types";

type Props = {
  media: MediaWithRelations;
};

export const EditMediaCoversUpload = ({ media }: Props) => {
  return <div>covers upload</div>;
};
